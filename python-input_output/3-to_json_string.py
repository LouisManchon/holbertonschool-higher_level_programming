#!/usr/bin/python3
"""
This module allow the function
"""


import json
def to_json_string(my_obj):
    """
    function that returns the JSON representation of an object (string)
    """
    with open(f.read, 'r', endcoding='utf-8') as f:
        json.my_obj = json.dumps(my_obj)
    return my_obj
